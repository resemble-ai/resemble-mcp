---
title: Create Identity
slug: /detect/identity/create
---

`POST https://app.resemble.ai/api/v2/identity`

Create a new voice identity profile based on an audio sample.

## Request Body

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `name` | string | ✅ | Speaker name/identifier. |
| `url` | string | ✅ | URL to audio sample of the speaker. |

### Example

```bash
curl --request POST 'https://app.resemble.ai/api/v2/identity' \
  -H 'Authorization: Bearer YOUR_API_TOKEN' \
  -H 'Content-Type: application/json' \
  --data '{
    "name": "John Doe",
    "url": "https://example.com/audio/john_doe_sample.wav"
  }'
```

## Response

```json
{
  "success": true,
  "item": {
    "uuid": "IDENTITY_UUID",
    "name": "John Doe",
    "created_at": "2024-01-01T00:00:00.000Z"
  }
}
```